services:
  application:
    build:
      context: ./
      dockerfile: Dockerfile.application
    ports:
      - target: 8081
        published: 8081
    environment:
      - RUNNING_ENV=DOCKER
    
  
  database:
    build: 
      context: ./
      dockerfile: Dockerfile.database
    environment:
      - RUNNING_ENV=DOCKER
